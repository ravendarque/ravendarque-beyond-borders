# See https://github.com/github/gitignore/blob/main/Node.gitignore and Vite defaults
# Dependencies
node_modules/

# Build output
/dist/
/.vite/
.vite-cache/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Package manager lock files (use pnpm only)
package-lock.json
yarn.lock

# pnpm ignores conflicting packages
node_modules/.ignored/

# Editor/OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Tests and coverage
coverage/
test-results/
.playwright/
# Playwright visual baseline snapshots (local only; regenerate with --update-snapshots)
*-snapshots/
test/e2e/**/*.spec.ts-snapshots/

# Local working files (scripts, outputs, temp files, etc.)
.local/
!.local/README.md

# Build artifacts
*.tsbuildinfo

# Env files (if used later)
.env
.env.*.local
